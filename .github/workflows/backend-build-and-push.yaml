name: Docker Image CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set github hash
        run: echo ${GITHUB_SHA::7} | tee /dev/tty | export GITHUB_HASH=-
      - name: Build the Docker image
        working-directory: ./backend
        run: docker build . --file Dockerfile --tag ghcr.io/LOCKhart07/snake-game:${GITHUB_HASH::7}
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.GITHUB_USERNAME }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Push the Docker image to GitHub Container Registry
        run: docker push ghcr.io/LOCKhart07/snake-game:${GITHUB_HASH}
